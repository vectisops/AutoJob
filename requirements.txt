# AutoJob — Python dependencies (Python 3.10+)
# After pip install, also run:  playwright install chromium
# Linux also needs: python3-tk  (and Chromium system libs via: playwright install-deps chromium)

customtkinter>=5.2.0
pillow>=10.0.0
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0
playwright>=1.40.0
pypdf>=4.0.0
python-docx>=1.1.0
tenacity>=8.2.0
